{
  "name": "ltc1043-sim",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "test": "node --test",
    "coverage": "node --test --experimental-test-coverage",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "npm test && npm run build"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
